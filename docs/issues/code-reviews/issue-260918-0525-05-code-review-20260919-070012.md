# Code review — ISSUE-260918-0525-05

Base: `a00eee1c0c86543d4031f883880fa18dcced7834` · Reviewers: claude, codex (session `01a0b86c-db2b-7932-8b9c-77c11f03084a`) · Contract: canonical

---

### L1. Argv stub is read before it has written every argument (FIXED)
> **Fix:** the stub appends a sentinel line after its argv; `argv_stub_args` returns nothing until the sentinel is last, then strips it. Stub-using test assertions unchanged.

**Source:** CODEX-4 (Claude agrees)
**Files:** `tests/integration.rs:2359-2385`

`write_argv_stub` appends one `printf` per argument, and `wait_for_argv_stub` returns on the first non-empty read. On a well-formed resume the argv is several tokens (for `claude` rich: `--permission-mode plan --resume <uuid>`), so an early read can see only a prefix. `flag_value(..., "--resume")` then returns `None`, and the CLI-seam resume tests fail at random.

**Required outcome:** the stub publishes its whole argv at once, for example by writing to a temp file and renaming it over the log, or by appending a sentinel line after the loop. The waiter returns only the complete argv, for example only once the sentinel is present, excluding the sentinel from the returned arguments. Every test that uses the stub keeps its assertions unchanged.
